# 🚀 تشغيل المشروع بسرعة

## الطريقة الأسهل (بدون Backend - Firebase فقط)

### 1. إعداد Firebase
راجع ملف `FIREBASE.md` لإنشاء مشروع Firebase

### 2. تحديث config.js
افتح `js/config.js` وضع بيانات Firebase الخاصة بك

### 3. تشغيل الموقع
```powershell
# افتح index.html مباشرة في المتصفح
# أو استخدم أي Web Server

# Python
python -m http.server 3000

# PHP
php -S localhost:3000

# Node.js
npx http-server -p 3000

# VS Code - Live Server Extension
# انقر بزر الأيمن على index.html → Open with Live Server
```

### 4. افتح المتصفح
```
http://localhost:3000
```

## مع Backend (خيار متقدم)

إذا أردت استخدام Backend Node.js:

```powershell
# 1. قاعدة البيانات
psql -U postgres -c "CREATE DATABASE seu_planner;"

# 2. Backend
cd backend
npm install
copy .env.example .env
# عدّل .env بمعلوماتك
npm run migrate
npm run seed
npm run dev

# 3. Frontend (في نافذة أخرى)
# افتح index.html في متصفح
# أو استخدم web server على port 3000
```

## الإعدادات المطلوبة

### في `js/config.js`:

```javascript
// لاستخدام Backend فقط (بدون Firebase)
const API_BASE_URL = 'http://localhost:5000/api';

// Firebase config (إذا أردت استخدامه)
const firebaseConfig = {
    apiKey: "ضع مفتاحك هنا",
    authDomain: "seu-planner.firebaseapp.com",
    // ... باقي الإعدادات
};
```

## المشاكل الشائعة

### ❌ CORS Error
**الحل**: استخدم web server (لا تفتح الملف مباشرة من file://)

### ❌ Firebase لا يعمل
**الحل**: تأكد من:
1. إعدادات Firebase صحيحة في config.js
2. تفعيل Email/Password في Firebase Console
3. الإنترنت متصل

### ❌ Backend لا يستجيب
**الحل**: تأكد من:
1. Backend يعمل على port 5000
2. قاعدة البيانات PostgreSQL تعمل
3. ملف .env صحيح

## اختبار سريع

### 1. تسجيل حساب
- افتح الموقع
- اضغط "إنشاء حساب جديد"
- أدخل بياناتك
- اختر تخصص

### 2. إنشاء خطة
- اضغط "إنشاء خطة دراسية"
- اختر 3-5 مواد
- اختر الترم الأول
- اضغط "إنشاء"

### 3. شاهد النتيجة
- جلسات اليوم
- التقويم
- الإحصائيات

## البيانات التجريبية

عند استخدام Backend، يوجد seed data:
- **12 تخصص**
- **89+ مادة**
- **ترمين دراسيين**

## الميزات المتاحة

✅ تسجيل دخول وحساب (Firebase أو Backend)
✅ اختيار التخصص
✅ اختيار المواد
✅ توليد خطة دراسية ذكية
✅ جلسات يومية
✅ تقويم أسبوعي
✅ إحصائيات
✅ إشعارات
✅ واجهة عربية كاملة

## الخطوات التالية

بعد التشغيل الناجح:
1. ✅ جرب كل الميزات
2. ✅ أضف مواد من تخصصك
3. ✅ خصص الإعدادات
4. ✅ شارك مع زملائك

---

**للمساعدة الكاملة**: راجع `SETUP.md` أو `README.md`
